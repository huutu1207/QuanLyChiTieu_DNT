// ProfileScreen.js
// Không có thay đổi gì ở file này, nó chỉ render ProfileComponents
import ProfileComponents from '../../components/ProfileComponents'; // Đường dẫn tới ProfileComponents

export default function ProfileScreen() {
  return <ProfileComponents />;
}